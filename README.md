# RiceHub website

Note:
To change the API URL you need to create a `config.js` file after building the website and place it in the same directory as `index.html` is. Example config file:

```javascript
window.__APP_CONFIG__ = {
    API_URL: "https://api.ricehub.io",
};
```
